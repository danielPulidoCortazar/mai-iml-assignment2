use the following commands to run the tests:

python main.py -d autos
python main.py -d vote
python main.py -d iris

for further analysis, run the playground.py
which can be finetuned to run isolated experiments with hyperparameters
it contains the clusteing quality indexes and confusion matrixes

dependencies:

scipy==1.7.3
numpy==1.21.6
pandas==1.3.5
scikit-learn==1.0.2
seaborn==0.12.2
matplotlib==3.5.3
